[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nim-rl"
version = "0.1.0"
description = "Tabular Q-learning experiments on the game of Nim"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
